

// This is the client entrypoint, makes sense that it can't be imported
// eslint-disable-next-line import/unambiguous
document.addEventListener("DOMContentLoaded", (): void => {

    console.log("load bitch");

});
